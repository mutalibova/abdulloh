services:
  - type: web
    name: platforma
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py migrate
      python manage.py collectstatic --no-input
    startCommand: gunicorn platforma.wsgi:application

    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: platforma.settings
      - key: SECRET_KEY
        value: django-insecure-...
      - key: DEBUG
        value: 'False'